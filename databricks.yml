bundle:
  name: model-deploy

include:
  - resources/model_deployment.yml

artifacts:
  default:
    type: whl
    build: uv build
    path: .

# ---------------------
# VARIABLES (TOP LEVEL)
# ---------------------
variables:
  git_sha:
    description: git_sha
    default: abcd

  branch:
    description: branch
    default: main

  schedule_pause_status:
    description: schedule pause status
    default: PAUSED

  model_name:
    description: Full UC MLflow Model Name
    default: mlops_dev.model_test.model_deploy     # ✔ MUST MATCH UC MODEL NAME


# ---------------------
# TARGETS
# ---------------------
targets:
  dev:
    default: true
    mode: development

    workspace:
      host: https://dbc-4c3ee4bb-030f.cloud.databricks.com/
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.target}/${bundle.name}
      profile: dbc-4c3ee4bb-030f

    variables:
      schedule_pause_status: UNPAUSED
      # If you want environment-specific model names:
      # model_name: mlops_dev.model_test.model_deploy

  acc:
    presets:
      name_prefix: "acc_"

    workspace:
      host: https://dbc-4c3ee4bb-030f.cloud.databricks.com/
      root_path: /Shared/.bundle/${bundle.target}/${bundle.name}
      profile: dbc-4c3ee4bb-030f

    variables:
      schedule_pause_status: UNPAUSED
      # model_name: mlops_dev.model_test.model_deploy

  prd:
    mode: production

    workspace:
      host: https://dbc-4c3ee4bb-030f.cloud.databricks.com/
      root_path: /Shared/.bundle/${bundle.target}/${bundle.name}
      profile: dbc-4c3ee4bb-030f

    variables:
      schedule_pause_status: UNPAUSED
      # model_name: mlops_dev.model_test.model_deploy
